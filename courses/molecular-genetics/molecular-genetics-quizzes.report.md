# molecular-genetics / quizzes — 65 questions

verified 60 · conflict 2 · external 2 · not-in-source 0 · needs-eye 1

**Conflicts** — the circulating file is wrong on these 2:

- **MGEN-Q-026** (ER-targeting signal sequence) — claimed "Signal Recognition
  sequence" (a). The source's own wording names the signal itself "peptide
  signal sequence" (d); the SRP is the particle that recognizes the signal, not
  the signal itself.
- **MGEN-Q-041** (DNA footprinting protection, True/False) — claimed False (a).
  Footprinting's entire premise is that a protein-bound site IS protected from
  nuclease cleavage — the statement is True (b).

**Disputed questions resolved** — 6 of Job A's disputed entries were decided
cleanly to one side rather than left in conflict: MGEN-Q-015 (model 1's
staggered-cut claim beat model 2's sequence-independence claim), MGEN-Q-016
(model 1's amino-acid-attachment claim beat model 2's "no function" claim),
MGEN-Q-017 (the hand-checkmark/model-2 pairing beat model 1's own stale Moodle
radio), MGEN-Q-023 (molecular-previous's mRNA→cDNA claim matched the source's
own framing more precisely than model 1's claim), MGEN-Q-029 (the English-UI
capture beat the Arabic-UI capture, which named the factually wrong start
codon AUC instead of AUG), and MGEN-Q-036 (the Sanger gel, decoded directly
from its own image — see below).

**MGEN-Q-036 in detail.** Five independent captures split 2 True / 3 False.
Decoding the gel image itself (bottom-to-top, per Ch.9's own reading rule)
gives the newly synthesized strand's sequence, 5'→3': GTACCCGAAATCAGGA — the
exact string the question calls "the template." Since the template is this
sequence's reverse complement, not the sequence itself, the statement is
False, matching the majority (3 of 5) claim; the two "True" claims both carry
a stale "Not yet answered" badge rather than a clean "Answer saved."

**External** — MGEN-Q-007 (H1 histone's role) and MGEN-Q-010 (definition of
processivity): both standard, settled molecular-biology facts, neither spelled
out in `source.md` beyond a passing mention of the term.

**Needs-eye** — MGEN-Q-044: the question's options are bare sample numbers
referencing an EcoR1 digest gel that was never captured anywhere in the raw
set (confirmed by Job A against the full-resolution original). No text in
`source.md` substitutes for the missing figure; the claim is transcribed but
unverifiable.

**Pedigree questions (11 of them, MGEN-Q-046–055, 059, 062, 065)** were all
verified directly from their `flagged/` crops using Ch.13's own
inference rules (dominant: every generation, affected parent required,
father-to-son transmission rules out X-linked; recessive: skips generations,
unaffected parents can have an affected child; X-linked recessive: only males
affected via carrier mothers; Y-linked: only males, direct male-line
transmission) rather than trusting the circulating claim first. All matched
their claimed answers except MGEN-Q-053, which Job A had marked needs-eye —
see below.

**MGEN-Q-053 upgraded from needs-eye.** Job A flagged it because 2 of 5
option letters were never captured and its own question number was inferred
by elimination. Neither issue blocks verification: the visible stem, figure,
and options a-c are complete, and the pedigree traces an unbroken
transmission chain through a phenotypically unaffected carrier (III-3) to an
affected daughter — a textbook dominant-trait-with-incomplete-penetrance
pattern that option a states outright. Verified rather than carried forward.

**Chapter coverage** — Ch.13 (Modes of Heredity) heaviest at 20 citations (all
pedigree and Mendelian-probability questions), Ch.9 (Molecular Techniques)
next at 19 (PCR/restriction enzymes/sequencing), Ch.8 (Translation) at 10,
Ch.1-2 (Replication) combined at 13, Ch.7 (Genetic Code) at 4, Ch.5
(Transcription) at 1 (the footprinting conflict), Ch.12 (Non-Mendelian) at 1
(incomplete penetrance). Ch.3, 4, 6, 10, 11, 14, 15, 16 untested by this tab.
